(rule
 (targets target_context.ml)
 (action
  (run config/discover.exe)))

(library
 (name dkml_probe)
 (public_name dkml-probe)
 (modules target_context)
 (libraries rresult))

(install
 (package dkml-probe)
 (files config/dkml_compiler_probe.h)
 (section share))
